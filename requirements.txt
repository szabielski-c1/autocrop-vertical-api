# OpenCV headless for server deployment (must come after scenedetect)
scenedetect
opencv-python-headless
ultralytics
torch
torchvision
tqdm
fastapi
uvicorn[standard]
python-multipart
celery[redis]
redis
requests
